<Project Sdk="Microsoft.NET.Sdk">

	<!-- Assembly metadata -->
	<PropertyGroup>
		<Title>Generic Modding Library</Title>
		<Company>YNHZRFXN</Company>
		<AssemblyVersion>0.2.3.1</AssemblyVersion>
		<FileVersion>0.2.3.1</FileVersion>
	</PropertyGroup>

	<!-- .NET options -->
    <PropertyGroup>
		<TargetFramework>net481</TargetFramework>
		<LangVersion>latest</LangVersion>
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<!-- Compiler settings -->
	<PropertyGroup>
		<AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
	</PropertyGroup>

	<!-- Determinism config -->
	<PropertyGroup>
		<Deterministic>true</Deterministic>
    	<DeterministicSourcePaths>true</DeterministicSourcePaths>
	</PropertyGroup>

	<ItemGroup>
    	<SourceRoot Include="$(MSBuildThisFileDirectory)/" />
	</ItemGroup>

	<!-- Referenced assemblies -->
	<ItemGroup>
		<Reference Include="../lib/Rain World/**/*.dll">
			<Private>false</Private>
		</Reference>
		<Reference Include="../lib/Mods/*.dll">
			<Private>false</Private>
		</Reference>
	</ItemGroup>

	<!-- Embedded resources -->
	<ItemGroup>
		<EmbeddedResource Include="../$(TargetName).Loader/patchers/$(TargetName).Loader.dll" LogicalName="$(TargetName).Loader.dll" />
	</ItemGroup>

	<!-- Post-compile tasks -->
	<Target Name="GenerateModLibrary" AfterTargets="PostBuildEvent">
		<ItemGroup>
			<CopyPlugins Include="../mod/newest/plugins/$(TargetName).*" />

			<CopyDeps Include="../lib/Mods/LogUtils.dll" />
			<CopyDeps Include="../lib/Mods/LogUtils.pdb" />

			<CopyRawFiles Include="$(TargetDir)$(TargetName).*" />
		</ItemGroup>

		<Exec Command="MonoMod.RuntimeDetour.HookGen.exe $(TargetDir)$(TargetName).dll" WorkingDirectory="../mod/utils/" />
		<Exec Command="ilrepack /out:../mod/newest/plugins/$(TargetName).dll /lib:../lib/Mods/ $(TargetDir)$(TargetName).dll $(TargetDir)MMHOOK_$(TargetName).dll" />

		<Copy SourceFiles="@(CopyPlugins)" DestinationFolder="../lib/Custom/" />
		<Copy SourceFiles="@(CopyDeps)" DestinationFolder="../mod/newest/plugins/" />
	</Target>

</Project>